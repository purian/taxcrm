<!DOCTYPE html>
<html lang="en" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>External Details</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f0f0f0;
      margin: 0;
      padding: 20px;
    }
    .container {
      max-width: 1200px;
      margin: 0 auto;
      background-color: #ffffff;
      padding: 20px;
      border-radius: 5px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
    }
    h1 {
      color: #333;
      text-align: center;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 20px;
    }
    th, td {
      padding: 12px;
      text-align: right;
      border-bottom: 1px solid #ddd;
    }
    th {
      background-color: #f2f2f2;
      font-weight: bold;
    }
    tr:hover {
      background-color: #f5f5f5;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>כל הפרטים החיצוניים</h1>

    <table>
      <thead>
        <tr>
          <th>מזהה</th>
          <th>שם הליד</th>
          <th>מזהה חברה</th>
          <th>מספר טלפון</th>
          <th>הערה</th>
          <th>תקף</th>
          <th>נוצר ב</th>
        </tr>
      </thead>
      <tbody>
        <% @external_details.each do |detail| %>
          <tr>
            <td><%= detail.id %></td>
            <td><%= detail.lead&.Name %></td>
            <td><%= detail.lead&.CompanyId %></td>
            <td><%= detail.phone_number %></td>
            <td><%= detail.comment %></td>
            <td><%= detail.is_valid ? 'כן' : 'לא' %></td>
            <td><%= detail.created_at.strftime('%Y-%m-%d %H:%M:%S') %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</body>
</html>